import React from "react";

const NoBookmark = () => (
  <div
    style={{
      height: "100%",
      display: "flex",
      flex: 1,
      lineHeight: 1,
      alignItems: "center",
      textAlign: "center",
      justifyContent: "center"
    }}
  >
    Chưa có bookmark nào
  </div>
);

export default NoBookmark;
