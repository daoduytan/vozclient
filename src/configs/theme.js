const colors = {
  background: "#f9f9f9",
  primary: "#1D364A",
  secondary: "#ED881F",
  dark: "#000",
  gray: "#888",
  border: "#eee"
};

const sizes = {
  fontSize: 16,
  lineHeight: 1.5,
  space: 15
};

const fonts = {
  size: sizes.fontSize,
  lineHeight: sizes.lineHeight
};

export default { colors, fonts, sizes };
